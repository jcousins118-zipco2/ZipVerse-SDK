# SDK surface

No client SDK has been published yet.

This directory is reserved for a future versioned client library that will help external tools:

- construct public task packets;
- validate packets against published schemas;
- submit authorised requests to a supported ZipVerse boundary;
- parse structured result envelopes.

The SDK will not contain the private ZipVerse engine or internal governance implementation.
